{
  "name": "bump-bumped",
  "private": true,
  "workspaces": [
    "packages/engine",
    "packages/client"
  ],
  "scripts": {
    "ci": "biome ci && npm run -w packages/client build && npm -w packages/engine test",
    "lint": "biome check",
    "format": "biome check --write"
  },
  "dependencies": {
    "phaser": "^4.2.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.4"
  }
}
